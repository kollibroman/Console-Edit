using System.Collections.Generic;
using csharpncurses;
using static csharpncurses.NCurses;

namespace txtEditor
{
    public class Buffer
    {
        public Buffer()
        {
            List<string> lines = new();

            InsertLine(string, );
        }
    }
}